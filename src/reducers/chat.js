export default (state = [], payload) => {

    // switch(payload.type) {
    //     case 'EMIT_MESSAGE':
    //         return state;
    //     default:
    //         console.log('default');
    //         return state;
    // }
}